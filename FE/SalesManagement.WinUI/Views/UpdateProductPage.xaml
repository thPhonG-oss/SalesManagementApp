<Page
    x:Class="SalesManagement.WinUI.Views.UpdateProductPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24"
          Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ===== HEADER ===== -->
        <StackPanel Orientation="Horizontal" Spacing="12">
            <Button Content="←"
                    Width="40"
                    Height="40"
                    Click="Back_Click"/>
            <TextBlock Text="Cập nhật sản phẩm"
                       FontSize="22"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <!-- ===== FORM ===== -->
        <ScrollViewer Grid.Row="1" Margin="0,20,0,0">
            <StackPanel Spacing="16">

                <TextBox Header="Tên sản phẩm"
                         Text="{Binding ProductName, Mode=TwoWay}"/>

                <TextBox Header="Tác giả / Nhà sản xuất"
                         Text="{Binding Author, Mode=TwoWay}"/>

                <NumberBox Header="Giá bán"
                           Value="{Binding PriceValue, Mode=TwoWay}" Grid.Row="5" Grid.Column="1"/>

                <NumberBox Header="Số lượng tồn kho"
                           Value="{Binding StockQuantity, Mode=TwoWay}" Grid.Row="5" Grid.Column="1"/>

                <TextBox Header="Mô tả"
                         Text="{Binding Description, Mode=TwoWay}"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         Height="100"/>


                <StackPanel Orientation="Horizontal"
                            Spacing="12"
                            Margin="0,20,0,0">
                    <Button Content="Lưu thay đổi"
                            MinWidth="120"
                            Style="{ThemeResource AccentButtonStyle}"
                            Click="Save_Click"/>

                    <Button Content="Hủy"
                            MinWidth="120"
                            Click="Back_Click"/>
                </StackPanel>

            </StackPanel>
        </ScrollViewer>

    </Grid>
</Page>
